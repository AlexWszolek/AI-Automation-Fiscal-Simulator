# Brief numbers — verified, with provenance

Generated by `scripts/brief_numbers.py` from the same `manifest.json` the report resolves
against, so these cannot drift from the model. **Regenerate after any rebuild.** The dotted
path is where to re-check each value.

Model build: `910978b`, generated 2026-07-27 18:58; Monte Carlo N=1000, seed 0.

| Figure | Value | Manifest path | Context |
|---|---:|---|---|
| Federal revenue baseline | 4,983 | `baselines.fed_revenue0_B` | $B/yr. Every federal % figure is a share of this. |
| State+local revenue baseline | 3,515 | `baselines.state_revenue0_B` | $B/yr. |
| Federal deficit baseline | 1,833 | `baselines.fed_deficit0_B` | $B/yr, CBO. Deltas are ON TOP of this. |
| Acemoglu-Modest: employment drop | 3 | `presets.acemoglu-modest.final.employment_drop_pct` | % at year 10. The mildest of the twelve presets. |
| Acemoglu-Modest: net fiscal impact | +12 | `presets.acemoglu-modest.final.net_fiscal_impact_B` | $B, final year. POSITIVE = the balance improves — at this mildness the recovery channels outweigh the losses. |
| Korinek-Suh AGI-5y: employment drop | 100 | `presets.agi-5y.final.employment_drop_pct` | % at year 10. The stress case. |
| Korinek-Suh AGI-5y: net fiscal impact | -3,091 | `presets.agi-5y.final.net_fiscal_impact_B` | $B, final year. Negative = the balance worsens. |
| Korinek-Suh AGI-5y: cumulative debt | 25,961 | `presets.agi-5y.final.fed_debt_B` | $B added over the horizon. |
| Windfall-Medium: 10y total revenue | -1.38 | `presets.windfall-medium.cumulative.cum10y_total_revenue_pct` | %. The direct comparator — set against their −2.8% in §9.1. |
| RAND replication: our federal revenue | -3.1 | `validation.rand_s3.fed_rev_pct_y10` | % below baseline at year 10, our accounting, against their −25%. |
| RAND replication: price-adjusted | -7.9 | `validation.rand_s3.fed_rev_pct_y10_nominal_adj` | % once revenue is scaled by our price level — the like-for-like number. |
| Acemoglu GDP check | 0.44 | `validation.acemoglu_gdp.y10_gdp_gain_pct` | % real output gain at year 10, inside his ~1.1% upper bound. |
| Screening: sample size | 10,000 | `screening.config.n` | Latin hypercube points over 26 dimensions, every one passing C1-C8. |
| Screening: worlds that improve | 6.0 | `screening.regimes.improves_pct` | %. NB policy is sampled here, so the rest of this block mixes AI harm with UBI outlay. |
| Screening: median deficit change | 1,478 | `screening.deficit_delta_final_B.p50` | $B. NOT the AI number — see the policy-pinned figure below, which is the honest one. |
| Screening: invariant failures | 0 | `screening.checks.invariant_failures` | Out of every sampled point. |

## Policy-pinned figures (the honest robustness numbers)

From `scripts/policy_sweep.py` — 19 uncertainty dims sampled, policy PINNED, so these
measure AI's fiscal effect rather than the cost of a sampled UBI. Source:
`docs/research/policy-sweep/ladder.csv`. Full argument and caveats in
[policy-potency-and-value-of-information.md](policy-potency-and-value-of-information.md).

| Figure | Value | Context |
|---|---:|---|
| Worlds where the balance worsens | 95.2% | No automation-side instrument, no UBI. The robustness claim. |
| Median deficit change | 414 $B | Final year, year 10. Use THIS, not the screening median. |
| 10% compute tax — already in the baseline | 26% of the hole | median 282 $B; fully closes it in 9% of worlds |
| compute tax at 20% | 53% of the hole | median 150 $B; fully closes it in 22% of worlds |
| compute tax at 40% | 106% of the hole | median -40 $B; fully closes it in 58% of worlds |
| automation tax at 50% of the capacity bound | 53% of the hole | median 129 $B; fully closes it in 29% of worlds |

**Do not quote `max-revenue` as a policy.** It taxes away all retained automation profit net of cost — it is in the sweep as an upper bound only.

## The automatic-stabiliser result

Spread of the median deficit across adoption-extent quintiles — how much the
outcome depends on how far automation actually goes:

- no instrument: **$851B** spread
- 10% compute tax: $626B
- 40% compute tax: **$59B** — near-flat: the tax base
  grows with the harm, so the instrument hedges the dominant uncertainty.

Caveat to carry with it: the hedge trades one unknown for another — under a compute
tax, `auto_cost` becomes the top driver, because it sets the size of the base.

## Credibility figures

- **Nine conservation identities** asserted every period of every run, including every
  Monte Carlo draw (C1 headcount, C2 disposition, C3 compute pool, C4 deflation,
  C5c funded raises, C6 federal reconciliation, C-sh shareholder ledger, C6-state/C7,
  C8 reduction).
- **C8 is exact**: with every behavioural lever off, the multi-actor system reproduces the
  static kernel bit-for-bit — float equality, differentially tested against v1.
- **366 regression tests**; the artifact pipeline is seeded and re-runs deterministically.
- **No preset is tuned to hit an external number.** §9 displays the disagreements with
  Windfall Trust, RAND and Acemoglu and decomposes each one.

## Known soft spots — fix before a reviewer finds them

- The README's "states bear an unfinanceable gap" overstates the central case: median
  cumulative state adjustment is ~$413B/10y against ~$15,026B federal.
- The screening median (+$1,478B) is substantially UBI outlay, not AI. Quote the
  policy-pinned median instead.
- No behavioural response to the tax instruments is modelled — no offshoring, no timing
  shift, no avoidance. The potency figures are therefore upper bounds.
