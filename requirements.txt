# Core model + interactive app (runs in the Python 3.12 venv).
#   uv pip install --python .venv/bin/python -r requirements.txt
# The offline PolicyEngine benefit bake has its own heavy deps — see requirements-bake.txt.
pandas>=2.2
numpy>=1.26
openpyxl>=3.1
pyarrow>=15          # parquet I/O for the cached lookups / interim artifacts
streamlit>=1.40      # interactive app (app/streamlit_app.py)

# Testing
pytest>=8.0
altair>=5,<7

# Report build (scripts/report_artifacts.py + scripts/build_report_docx.py)
vl-convert-python>=1.9   # altair -> PNG figures (pin family: PNG byte determinism per version)
python-docx>=1.1         # docx assembly
