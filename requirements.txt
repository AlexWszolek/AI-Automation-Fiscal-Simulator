# Core model + interactive app (runs in the Python 3.12 venv).
#   uv pip install --python .venv/bin/python -r requirements.txt
# The offline PolicyEngine benefit bake has its own heavy deps — see requirements-bake.txt.
#
# NATIVE STACK IS PINNED EXACTLY. Streamlit Community Cloud re-resolves this file from scratch on
# every deploy; leaving numpy/pandas/pyarrow unpinned let it assemble an ABI-mismatched trio that
# SIGSEGV'd (segfault, no Python traceback) in native code on the dataframe/Arrow render path when
# switching scenarios. These are the exact versions the working local venv runs — keep in lockstep.
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0          # Arrow serialization behind st.dataframe + parquet I/O
openpyxl==3.1.5
streamlit==1.58.0        # interactive app (app/streamlit_app.py)

# Testing
pytest==8.4.2
altair==6.2.1

# Report build (scripts/report_artifacts.py + scripts/build_report_docx.py)
vl-convert-python==1.9.0.post1   # altair -> PNG figures (pin family: PNG byte determinism per version)
python-docx==1.2.0               # docx assembly
